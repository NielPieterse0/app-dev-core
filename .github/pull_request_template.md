<!-- The PR is the change record. There is no tasks.md / receipt / checklist file. -->
<!-- GENERATED by governance/compile.ts from governance/rules.ts. DO NOT EDIT. Run `npm run governance:compile`. -->

`Conformance: Lean ? | Efficient ? | Effective ? — <justification, or the trade made>`

<!-- Required [R00]. Empty = blocked merge. -->

---

**Class:** direct | planned | gated · **Compatibility:** patch | minor | major

**Origin:** core | product:<name> · **Type:** correction | proposal

**Problem:**

**Acceptance condition:**

## Evidence

- [ ] Red observed for the intended reason — `<cmd>`
- [ ] Green — `<cmd>`
- [ ] Hosted CI green on this commit, checks dispatched > 0 — `<link>` [R05]

## Machine-checked for you

These 16 rules are enforced by `npm run verify`. You do not need to confirm them.
If CI is green, they hold. If you are tempted to tick them by hand, that is the defect.

- `R02` No sibling-path (`../../`) dependency in build, test, CI, deploy or release.
- `R04` The core contains no product source, migrations or feature specs.
- `R06` Workflows live at the repository-root `.github/workflows/`. Nested paths are invisible to Actions.
- `R10` Tooling is Node/TypeScript. No .ps1, .sh, .bat or .cmd.
- `R11` Toolchain pinned. `latest` and `lts/*` are prohibited.
- `R12` No machine-local absolute paths in committed artifacts.
- `R13` Authorization is tested against a real local Postgres, including negative cases. A mocked client cannot prove RLS.
- `R18` No tasks.md, workflow-receipts.md, checklist.md, plans/, audit/ or agent logs. The change record is the pull request.
- `R20` Normative language appears only in the Operating Model. Elsewhere, cite a rule; never restate it.
- `R21` AGENTS.md is at most 80 lines. It routes; it does not restate. It loads on every task.
- `R22` The declared Codex permission profile exists. Documentation never restates it.
- `R24` No artifact outside the directory grammar. A new artifact type requires a grammar amendment first.
- `R23` Every CI rule has a passing fixture and at least one failing fixture.
- `R37` The product manifest schema keeps the baseline contract honest: core, operatingModel and archetype stay required.
- `R39` Published core changes live in append-only core/releases.json entries with a valid category and minimum release metadata.
- `B1` capabilities/ stays empty until two products independently prove a need.

## Rollback

## Deviations

<!-- none, or a record with an expiry -->
