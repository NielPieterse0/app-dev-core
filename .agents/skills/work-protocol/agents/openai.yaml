interface:
  display_name: "Work Protocol"
  short_description: "Run app-dev-core changes correctly"
  default_prompt: "Use $work-protocol to classify, verify, and close out this app-dev-core change."
