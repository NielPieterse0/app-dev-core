# react-vite-capacitor

Generated product files for this archetype live under `template/`.
The generator restores reserved path aliases there, including `.github/` and `db/migrations/`, when it emits a product repository.
