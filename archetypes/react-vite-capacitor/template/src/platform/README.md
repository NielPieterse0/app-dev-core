Platform-specific code lives here.

- `composition.ts` is the single place that names concrete adapters.
- `web/adapters/` is populated in the Stage 1 seed.
- `native/`, `ios/`, `android/`, and `desktop/` stay empty until a real delivery
  requirement activates those slots.
