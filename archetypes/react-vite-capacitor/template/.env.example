# No backend is wired in this archetype seed.
# When a product adds one:
# - keep this file placeholders-only
# - route session/storage concerns through capability ports
# - use flowType: 'pkce' if the auth flow is browser Supabase
#
# VITE_API_URL=
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
